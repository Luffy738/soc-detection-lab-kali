Wireshark Filters Used:

1. SSH Traffic
tcp.port == 22

2. TCP SYN Packets
tcp.flags.syn == 1 && tcp.flags.ack == 0

Observations:
- Repeated TCP SYN packets
- Multiple short-lived SSH handshake attempts
- Rapid reconnection behavior

Conclusion:
Network traffic confirms automated brute-force activity.
